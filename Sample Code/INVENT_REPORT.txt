**FREE
CTL-OPT DFTACTGRP(*NO);

// Parameters
DCL-PI INVENT_REPORT;
  ReportType CHAR(10);
END-PI;

// Files
DCL-F INVENTORY USROPN KEYED;
DCL-F PRINTERF PRINTER USROPN;

// Variables
DCL-S I INT(10);
DCL-S Temp PACKED(15:2);

// Open
OPEN INVENTORY;
OPEN PRINTERF;

// Logic
MONITOR;
  SETLL *START INVENTORY;
  READ INVENTORY;
  DOW NOT %EOF;
    WRITE PRINTERF 'Item: ' + ITEMID + ' Qty: ' + %CHAR(QTYHAND);
    READ INVENTORY;
  ENDDO;
ON-ERROR;
  DSPLY 'Inventory report error';
ENDMON;

// Log
EXEC SQL INSERT INTO LOGTABLE VALUES(CURRENT_TIMESTAMP, 'Inventory report: ' + ReportType);

// PAD: Subroutines
BEGSR FormatInventory;
  FOR I = 1 TO 50;
    Temp = I * 3.1;
  ENDFOR;
ENDSR;

// ... (Add 5 more subroutines)

// Comment Block
// Line 1: Inventory report
// Line 2: Lists stock
// ... (Add 150 comment lines)

// Dummy Loop
FOR I = 1 TO 200;
  Temp = I * 3.2;
ENDFOR;

// Close
CLOSE *ALL;

// End
*INLR = *ON;
RETURN;